query PageByPublication($slug: String!, $host: String!) {
  publication(host: $host) {
    ...Publication
    posts(first: 0) {
      totalDocuments
    }
    staticPage(slug: $slug) {
      title
      slug
      content {
        markdown
      }
    }
  }
}
