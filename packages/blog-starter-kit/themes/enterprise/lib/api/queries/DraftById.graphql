query DraftById($id: ObjectId!) {
  draft(id: $id) {
    id
    title
    content {
      markdown
    }
    author {
      id
      name
      username
      profilePicture
    }
    coverImage {
      url
    }
    updatedAt
    tags {
      id
      name
      slug
    }
  }
}
