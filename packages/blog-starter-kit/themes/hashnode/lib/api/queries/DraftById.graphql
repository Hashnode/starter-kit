query DraftById($id: ObjectId!) {
  draft(id: $id) {
    id
    title
    content {
      markdown
    }
    coverImage {
        url
    }
    author {
      id
      name
      username
      profilePicture
    }
    dateUpdated
    tags {
      id
      name
      slug
    }
  }
}
